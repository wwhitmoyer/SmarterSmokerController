name: smarter_smoker
description: Cross-platform Smarter Smoker controller using MQTT with BLE-assisted discovery.
publish_to: none
version: 0.3.0+3

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: 2.3.10
  mqtt_client: 10.11.11
  shared_preferences: 2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
